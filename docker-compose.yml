version: "3.7"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./package.json:/app/package.json
      - ./src/:/app/src
    ports:
      - 3000:3000
    restart: always
